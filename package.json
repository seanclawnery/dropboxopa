{
  "name": "dropbox-oauth-spa",
  "version": "1.0.0",
  "description": "Secure single page app for Dropbox OAuth authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "keywords": ["dropbox", "oauth", "spa", "authentication"],
  "author": "Sean Clawnery",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}